[pytest]
markers =
    provider(name): mark test as specific to a given quantum provider (e.g. qiskit, cirq, braket, azure, myqlm, quantinuum)
; culprits packages added as comments
filterwarnings = 
    ;  pyreadline
    ignore::DeprecationWarning
